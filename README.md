# About

This is just a simple SSH client (mostly for testing/diagnostic purposes).

# Quick start

### Build for ARM64
```sh
git clone https://github.com/ChronoMonochrome/go-ssh-client
cd go-ssh-client
CGO_ENABLED=0 GOARCH=arm64 go build
```
